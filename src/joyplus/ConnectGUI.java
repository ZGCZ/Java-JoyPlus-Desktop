package joyplus;

import javax.swing.JFrame;

public class ConnectGUI extends JFrame {
    public ConnectGUI() {
        setTitle("Connect");
        setSize(300, 200);
        setLocationRelativeTo(null);
        setAlwaysOnTop(true);
    }
}
